# Sazinka Worker Configuration

# NATS server URL
NATS_URL=nats://localhost:4222

# PostgreSQL connection string
DATABASE_URL=postgres://sazinka:sazinka_dev@localhost:5432/sazinka

# Geocoding backend: "mock" or "nominatim"
GEOCODER_BACKEND=nominatim

# Nominatim API URL (for geocoding)
# Use local: http://localhost:8080 (run `docker compose up nominatim` in infra/)
# Use public: https://nominatim.openstreetmap.org (rate limited!)
NOMINATIM_URL=http://localhost:8080

# Nominatim rate limiting (milliseconds between requests)
# Local: 100ms is fine, Public: 1500ms minimum required
NOMINATIM_RATE_LIMIT_MS=100

# Valhalla routing engine URL (optional, falls back to mock if unavailable)
# Run `docker compose up valhalla` in infra/ to start
VALHALLA_URL=http://localhost:8002

# Logs directory (relative to worker binary)
LOGS_DIR=../logs

# Logging level
RUST_LOG=info,sazinka_worker=debug
