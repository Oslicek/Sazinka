---
// Root redirect based on browser language
// Falls back to /en/ if JavaScript is disabled
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="refresh" content="0;url=/en/" />
    <title>Ariadline — Redirecting...</title>
    <script is:inline>
      (function () {
        var lang = (navigator.language || '').toLowerCase().slice(0, 2);
        var locales = { cs: '/cs/', sk: '/sk/' };
        window.location.replace(locales[lang] || '/en/');
      })();
    </script>
  </head>
  <body>
    <p>Redirecting to <a href="/en/">English version</a>...</p>
  </body>
</html>
